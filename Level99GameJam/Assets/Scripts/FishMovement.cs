using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using DG.Tweening;

public class FishMovement : MonoBehaviour
{
    // swim random direction
    //rotate towards new direction it will go
    //repeat

    //needs to stay within bounds (fish zone?)



    // Fish Swimming animation work. I might come back to this if I have time to polish it

    //private void Start()
    //{
    //    Vector3 currentTarget;


    //        Debug.Log("Fish is swimming");
    //        currentTarget = GetRandomDirection();

    //        transform.DOLookAt(currentTarget, 1f, AxisConstraint.None, Vector3.up);
    //        transform.DOBlendableMoveBy(GetRandomDirection(), 5f, false);


    //}

    //private Vector3 GetRandomDirection()
    //{
    //    return new Vector3(Random.Range(.2f, 3f), 0, Random.Range(.2f, 3f));
    //}
}
